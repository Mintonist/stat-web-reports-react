1. проследить, как обернуты в UserProvider, DepartProvider, ReportProvider, почему перерисовывется в компонентах без setState()
2. почему дважды вызывается, например, user.api.fetchAll()
3. как правильно показать загрузку в реализации с провайдерами (ранее я показывал в каждом компоненте - сейчас не имеет смысла)
4. как правильно отделить видимые текущему пользователю отчёты (сейчас через useReports.filterReports())
5. почему нельзя спользовать await напрямую в useEffect()?
6. почему так странно передаются видимые извне методы для useContex()?
7. почему перестал работать редирект с / на /main ?


// единожды создаём образ
docker build -t diplom-image .

// список всех образов
docker image ls

// запускаем контейнер на основе образа с редиректом портов: ПОРТ_СНАРУЖИ:ПОРТ_ВНУТРИ
docker run -d -p 8081:8081 --name diplom-container --rm diplom-image

// список запущенных контейнеров
docker ps -a