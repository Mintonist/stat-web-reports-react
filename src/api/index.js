import users from './fake.api/users.api';
import reports from './fake.api/reports.api';
import departs from './fake.api/departs.api';

const API = {
  users,
  reports,
  departs,
};
export default API;
